[tool.poetry]
name = "testpackage"
version = "0.1.0"
description = ""
authors = ["TruthWatcher <danielalansaunders@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
